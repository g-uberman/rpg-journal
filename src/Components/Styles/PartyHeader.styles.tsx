import styled from "styled-components";

export const StyledPartyHeader = styled.div`
  text-align: center;
  cursor: pointer;
  margin: 0 auto;
  font-size: 1.5em;

  /* h1::before {
    content: "†";
    transform: rotate(90deg);
    position: absolute;
    left: -40px;
    font-weight: initial;
  }

  h1::after {
    content: "†";
    transform: rotate(-90deg);
    position: absolute;
    right: -40px;
    font-weight: initial;
  } */
`;
